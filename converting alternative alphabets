#include<stdio.h>
#include<conio.h>
#include<string.h>
int main()
{
	int v=1,i;
	char str[100];
	printf("enter the string:");
	gets(str);
	for(i=0;i<=strlen (str);i++)
	{
		if(i%2!=0)
		{
			str[i]=str[i]-32;
		}
		printf("%c",str[i]);
		
	}
}
